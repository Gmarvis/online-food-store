import React from "react";
import { useFormik } from "formik";

export const Checkout = () => {
  return (
    <div>
      <form>
        <label>name on card</label>
        <input></input>
      </form>
    </div>
  )
};
